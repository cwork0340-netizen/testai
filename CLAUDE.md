# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is a personal AI-learning sandbox (`testai` / 學習ai). At the time of writing it contains only `README.md` and this file — no source code, build system, tests, or tooling configuration exists yet. There are no Cursor rules, Copilot instructions, or package manifests to inherit conventions from.

When the user adds code, update this file with the real build / lint / test commands and the high-level architecture instead of leaving the placeholder above.

## Review workflow

Codex 會在你完成後審查你的輸出。
