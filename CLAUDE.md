# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 關於這個 repo

`testai`（學習 ai）是擁有者的個人學習沙盒，**擁有者非工程師背景**。目前還沒有任何程式碼、建置系統或測試框架，只有 `README.md` 與本檔案。隨著學習進度，內容可能會涵蓋：

- AI / Prompt 使用技巧（與 Claude、ChatGPT 等對話、寫 prompt）
- Python 程式（語法練習、小工具、跟著教學做）
- 網頁 / JavaScript（HTML、CSS、JS、小應用）
- 其他主題（資料分析、機器學習等，可由 Claude 主動推薦）

## 與擁有者協作的方式

- **語氣**：助手風，簡潔直接。擁有者說什麼就做什麼，只在重點處說明，不需要每步都解釋原理。
- **語言**：所有對話、說明、commit message、錯誤解釋一律使用**繁體中文**。
- **背景假設**：擁有者非工程師，因此：
  - 提到專有名詞（例如 `dependency`、`runtime`、`stack trace`）時，第一次出現要簡短附上中文意思。
  - 不要假設擁有者熟悉 CLI、git、套件管理工具的細節，必要時用一兩句話帶過。

## 寫程式時的規範

- **註解**：只在關鍵步驟或邏輯較複雜的地方加註解，註解使用繁體中文。不要每行都加，也不要寫廢話註解。
- **練習導向**：擁有者是「跟 Claude 一起寫程式練習」的模式，因此：
  - Claude 寫完後要簡短說明「這段在做什麼」，讓擁有者學得到東西。
  - 如果有更簡單或更常見的寫法，可主動指出，但不要每次都重構。
- **不要過度抽象**：學習階段優先選好讀的寫法，不要為了「未來擴充」而加額外的設計。

## 遇到錯誤訊息

當執行出錯、看到 stack trace、編譯錯誤、套件衝突時：

1. **先用繁體中文解釋這個錯誤大致是什麼意思**（一兩句即可）。
2. 列出**可能的原因**與**建議的處理方式**。
3. **再問擁有者要怎麼處理**，不要直接動手改。
4. 範例輸出格式：
   > 這個錯誤是說「找不到 numpy 這個套件」。通常代表還沒安裝。
   > 可能的做法：
   > 1. 用 `pip install numpy` 裝起來
   > 2. 換用內建的套件改寫
   > 你想用哪個？

## Git / commit / push 流程

- **不要自動 commit**。改完檔案後，要先跟擁有者**口頭確認**改動內容，由擁有者說「commit 吧」或「推上去」再執行。
- commit message 使用繁體中文，**一行簡短說明做了什麼**即可，不需要落落長。
- push 到遠端前再次確認分支名稱與目標。
- 不要在沒被要求時開 Pull Request。

## 審查機制

Codex 會在你完成後審查你的輸出。寫完程式或修改後，記得自己先檢查：

- 有沒有保留沒用到的程式碼或 `print` 除錯訊息？
- 變數命名是否易讀？
- 註解是否還對應現在的程式邏輯？
- 中文說明有沒有錯字或不通順的地方？
